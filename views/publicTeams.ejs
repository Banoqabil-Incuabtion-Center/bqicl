<%- include('layouts/public_header') %>

    <main class="max-w-7xl mx-auto px-4 py-12 flex-grow w-full">
        <header class="text-center mb-16">
            <h1 class="text-5xl md:text-7xl font-black italic uppercase icl-gradient-text mb-4">The Franchises</h1>
            <p class="text-gray-600 dark:text-gray-400 font-medium">Behold the legendary teams battling for the
                championship.</p>
        </header>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
            <% teams.forEach(team=> { %>
                <div
                    class="team-card glass rounded-[2.5rem] p-8 flex flex-col items-center text-center group hover:bg-lime-500/5 transition-all border border-black/5 dark:border-white/5 hover:border-lime-500/20">
                    <div
                        class="team-logo w-40 h-40 bg-white rounded-3xl mb-8 flex items-center justify-center p-4 shadow-2xl transition-transform duration-500 overflow-hidden relative">
                        <img src="<%= team.teamLogo || '/images/default-team.png' %>" alt="<%= team.name %>"
                            class="w-full h-full object-contain group-hover:scale-110 transition-transform duration-500">
                    </div>
                    <h3 class="text-2xl font-black italic uppercase mb-2 tracking-tight text-gray-900 dark:text-white">
                        <%= team.name %>
                    </h3>
                    <div class="h-1 w-12 bg-lime-500 rounded-full mb-6"></div>

                    <div
                        class="w-full bg-black/5 dark:bg-white/5 rounded-2xl p-4 border border-black/5 dark:border-white/5">
                        <p class="text-[10px] font-black uppercase tracking-[0.2em] text-gray-500 mb-1">Franchise
                            Owner
                        </p>
                        <p class="text-sm font-bold text-gray-700 dark:text-gray-200">
                            <%= team.owner ? team.owner.name : 'Unassigned' %>
                        </p>
                    </div>

                    <a href="/teams/<%= team.hashedId %>"
                        class="mt-8 text-[10px] font-black uppercase tracking-widest text-lime-400 hover:text-lime-300 transition-colors flex items-center gap-2">
                        View Squad <i class="fas fa-arrow-right text-[8px]"></i>
                    </a>
                </div>
                <% }) %>
        </div>
    </main>

    <%- include('layouts/public_footer') %>