<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= player.name %> | Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap');
        body { font-family: 'Outfit', sans-serif; background-color: #020617; }
        .glass-panel { background: rgba(15, 23, 42, 0.6); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.05); }
    </style>
</head>

<body class="text-slate-200 antialiased">
    <div class="flex flex-col md:flex-row min-h-screen">
        
        <div class="shrink-0 bg-slate-900 border-b md:border-r border-slate-800">
            <% if (locals.userType === 'admin') { %>
                <%- include('partials/adminNavbar') %>
            <% } else if (locals.userType === 'owner') { %>
                <%- include('partials/ownerNavbar') %>
            <% } %>
        </div>

        <main class="flex-1 p-4 md:p-8 overflow-y-auto">
            
            <div class="flex justify-between items-center mb-8">
                <button onclick="history.back()" class="text-slate-400 hover:text-white flex items-center gap-2 group">
                    <i class="fas fa-chevron-left group-hover:-translate-x-1 transition-transform"></i> Back
                </button>
                <% if (locals.userType === 'admin') { %>
                    <a href="/player/profile/edit/<%= player.hashedId %>" class="bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded-lg text-sm transition-all shadow-lg shadow-blue-900/20">
                        Edit Player
                    </a>
                <% } %>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-12 gap-6 items-start">
                
                <div class="md:col-span-5 lg:col-span-4 space-y-6">
                    <div class="glass-panel rounded-3xl p-6 text-center md:text-left overflow-hidden relative">
                        <% 
                            let accentColor = "blue";
                            if(player.auctionCategory === "Platinum") accentColor = "blue";
                            else if(player.auctionCategory === "Diamond") accentColor = "purple";
                            else if(player.auctionCategory === "Gold") accentColor = "yellow";
                        %>
                        <div class="absolute top-0 right-0 -mt-8 -mr-8 w-32 h-32 bg-<%= accentColor %>-500/10 blur-3xl rounded-full"></div>

                        <div class="relative">
                            <div class="w-32 h-32 md:w-full aspect-square mx-auto md:mx-0 rounded-2xl overflow-hidden border-2 border-slate-700 mb-6 shadow-2xl">
                                <% if (player.playerImage) { %>
                                    <img src="<%= player.playerImage %>" class="w-full h-full object-cover">
                                <% } else { %>
                                    <div class="w-full h-full bg-slate-800 flex items-center justify-center text-5xl font-bold"><%= player.name.charAt(0) %></div>
                                <% } %>
                            </div>

                            <span class="inline-block px-3 py-1 bg-<%= accentColor %>-500/20 text-<%= accentColor %>-400 text-[10px] font-black tracking-widest rounded-full border border-<%= accentColor %>-500/30 mb-2 uppercase">
                                <%= player.auctionCategory %> Tier
                            </span>
                            <h1 class="text-3xl md:text-4xl font-extrabold text-white mb-2 leading-tight"><%= player.name %></h1>
                            <p class="text-slate-400 font-medium mb-6"><%= player.category %></p>
                            
                            <% if (player.isSold) { %>
                                <div class="bg-emerald-500/10 border border-emerald-500/20 rounded-xl p-4 text-emerald-400">
                                    <p class="text-[10px] font-bold uppercase mb-1 opacity-60">Auction Status</p>
                                    <p class="font-bold flex items-center gap-2">
                                        <i class="fas fa-check-circle"></i> SOLD TO <%= player.teamId %>
                                    </p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>

                <div class="md:col-span-7 lg:col-span-8 space-y-6">
                    
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                        <div class="glass-panel rounded-2xl p-5 flex items-center gap-4">
                            <div class="w-12 h-12 bg-yellow-500/10 rounded-xl flex items-center justify-center text-yellow-500 shrink-0">
                                <i class="fas fa-tag text-lg"></i>
                            </div>
                            <div class="min-w-0">
                                <p class="text-[10px] text-slate-500 font-bold uppercase tracking-widest">Base Price</p>
                                <p class="text-xl font-bold text-white truncate">$<%= player.basePrice ? player.basePrice.toLocaleString() : '0' %></p>
                            </div>
                        </div>
                        <div class="glass-panel rounded-2xl p-5 flex items-center gap-4">
                            <div class="w-12 h-12 bg-blue-500/10 rounded-xl flex items-center justify-center text-blue-500 shrink-0">
                                <i class="fas fa-university text-lg"></i>
                            </div>
                            <div class="min-w-0">
                                <p class="text-[10px] text-slate-500 font-bold uppercase tracking-widest">Campus</p>
                                <p class="text-xl font-bold text-white truncate"><%= player.campus || 'Not Set' %></p>
                            </div>
                        </div>
                    </div>

                    <div class="glass-panel rounded-3xl p-6">
                        <h3 class="text-sm font-bold text-slate-500 uppercase tracking-widest mb-6">Playing Statistics</h3>
                        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                            <div class="bg-slate-900/40 rounded-2xl p-4 border border-slate-800">
                                <div class="flex items-center gap-2 text-yellow-500 mb-4 font-bold text-xs uppercase">
                                    <i class="fas fa-bat"></i> Batting
                                </div>
                                <div class="grid grid-cols-2 gap-2">
                                    <div>
                                        <p class="text-[9px] text-slate-500 uppercase font-bold">Style</p>
                                        <p class="text-sm font-semibold"><%= player.playingStyle %></p>
                                    </div>
                                    <div class="text-right">
                                        <p class="text-[9px] text-slate-500 uppercase font-bold">Order</p>
                                        <p class="text-sm font-semibold"><%= player.battingOrder || 'Open' %></p>
                                    </div>
                                </div>
                            </div>
                            <div class="bg-slate-900/40 rounded-2xl p-4 border border-slate-800">
                                <div class="flex items-center gap-2 text-red-500 mb-4 font-bold text-xs uppercase">
                                    <i class="fas fa-bowling-ball"></i> Bowling
                                </div>
                                <div class="grid grid-cols-2 gap-2">
                                    <div>
                                        <p class="text-[9px] text-slate-500 uppercase font-bold">Type</p>
                                        <p class="text-sm font-semibold truncate"><%= player.bowlingType || 'N/A' %></p>
                                    </div>
                                    <div class="text-right">
                                        <p class="text-[9px] text-slate-500 uppercase font-bold">Specialty</p>
                                        <p class="text-sm font-semibold"><%= player.bowlingType && player.bowlingType.includes("Spin") ? "Spinner" : "Pacer" %></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="glass-panel rounded-3xl p-6">
                        <div class="flex flex-col sm:flex-row gap-8">
                            <div class="flex items-center gap-4">
                                <div class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center text-slate-400 shrink-0">
                                    <i class="fas fa-envelope"></i>
                                </div>
                                <div class="min-w-0">
                                    <p class="text-[10px] text-slate-500 uppercase font-bold">Email</p>
                                    <p class="text-sm font-medium truncate text-slate-300"><%= player.email %></p>
                                </div>
                            </div>
                            <div class="flex items-center gap-4">
                                <div class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center text-slate-400 shrink-0">
                                    <i class="fas fa-phone"></i>
                                </div>
                                <div>
                                    <p class="text-[10px] text-slate-500 uppercase font-bold">Phone</p>
                                    <p class="text-sm font-medium text-slate-300"><%= player.phoneNumber || 'N/A' %></p>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </main>
    </div>
</body>

</html> -->

